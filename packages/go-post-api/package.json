{
  "name": "go-post-api",
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "ganache-cli": "node_modules/.bin/ganache-cli -m 'quarter amazing fetch shove salute seminar injury vivid awful area street surge' --gasLimit 136500000",
    "migrate:local": "truffle migrate --network=development --reset --all",
    "migrate:test": "truffle migrate --network=test --reset --all",
    "migrate:main": "truffle migrate --network=main --reset --all",
    "test": "scripts/test.sh test"
  },
  "devDependencies": {
    "truffle": "^4.1.14",
    "truffle-privatekey-provider": "1.1.0",
    "web3": "1.0.0-beta.36"
  },
  "dependencies": {
    "bn.js": "4.11.8",
    "ganache-cli": "^6.2.1"
  }
}
